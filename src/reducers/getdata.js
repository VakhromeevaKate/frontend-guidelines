const initialState = {
    colors: {
        header: 'black',
        footer: 'yellow'
    },
    fonts: {
        header: 'Verdana',
        paragraph: 'Arial'
    }
};

export default function data(state = initialState) {
    return state;
}